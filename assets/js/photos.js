/* =========================================================================
   Photographs.

   To add one:
     1. Drop the original file into  assets/photos/  (originals only, no
        resizing or compression)
     2. Add a line below. Newest first, since the page keeps this order.

   The section hides itself while this list is empty, so you can ship it
   half-finished without an awkward gap on the page.
   ========================================================================= */

window.PHOTOS = [
  // { src: 'assets/photos/example.jpg', caption: 'Berlin', year: '2025' },
];
